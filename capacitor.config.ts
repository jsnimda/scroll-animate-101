import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scroll-animate-101',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
